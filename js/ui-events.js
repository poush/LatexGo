var footercb = document.getElementById("compile");

var event = new Event('spinner');

// Listen for the event.
footercb.addEventListener('spinner', function(compile) {
    setInterval(function () {
        .loader {
          border-top: 16px solid blue;
          border-bottom: 16px solid blue;
        }
    });
});
